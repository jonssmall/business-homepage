node_modules/

# OS / editor noise
.DS_Store
Thumbs.db
*.swp
.vscode/
.idea/

# Secrets — never commit real values. Copy to .env.local and fill in
# if this project ever needs one (contact-form keys, API tokens, etc).
.env
.env.local
.env.*.local
*.pem
*.key

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
